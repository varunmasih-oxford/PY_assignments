## 1. Even or Odd Checker

Write a program that takes a number and prints whether it is **even or odd** using `if-else`.

---

## 2. Largest of Three Numbers

Take three numbers as input and print the **largest number** using nested `if`.

---

## 3. Grade Calculator

Input marks and print grade:

* 90+ → A
* 75–89 → B
* 50–74 → C
* Below 50 → Fail

(Use `if-elif-else`)

---

## 4. Day Name using match-case

Take a number (1–7) and print the **day name** using `match-case`.

---

## 5. Positive, Negative or Zero

Check whether a number is:

* Positive
* Negative
* Zero

---

## 6. Sum of First N Numbers

Use a `for` loop with `range()` to calculate sum of first `n` numbers.

---

## 7. Multiplication Table

Print multiplication table of a given number using a `for` loop.

---

## 8. While Loop Countdown

Write a program to print numbers from 10 to 1 using a `while` loop.

---

## 9. Skip Multiples of 3

Print numbers from 1 to 20 but **skip multiples of 3** using `continue`.

---

## 10. Stop at First Negative Number

Take numbers from a list and stop the loop when a **negative number appears** using `break`.

Example list:

```python
[5, 8, 12, -3, 7, 10]
```

