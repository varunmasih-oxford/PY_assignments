## 1. Basic for Loop
Write a program that:
- Uses `for` loop with `range()`  
- Prints numbers from 0 to 9  

---

## 2. Custom Range Loop
Write a program that:
- Uses `range(5, 15, 2)`  
- Prints all values generated  

---

## 3. While Loop Practice
Write a program that:
- Initializes a variable `count = 0`  
- Uses `while` loop to print numbers from 0 to 9  

---

## 4. Reverse While Loop
Write a program that:
- Starts from `num = 10`  
- Prints numbers in reverse (10 to 1) using `while` loop  

---

## 5. Break Statement
Write a program that:
- Uses a `for` loop from 0 to 10  
- Stops the loop when value becomes 6  
- Prints values before stopping  

---

## 6. Continue Statement
Write a program that:
- Uses a `for` loop from 0 to 10  
- Skips printing when value is 5  
- Prints all other values  

---

## 7. Pass Statement
Write a program that:
- Uses an `if` condition (`if 20 > 10`)  
- Uses `pass` inside it  
- Prints a message after the condition  

---

## 8. Even Numbers Using Loop
Write a program that:
- Uses `for` loop  
- Prints all even numbers from 1 to 20  

---

## 9. Sum Using While Loop
Write a program that:
- Uses `while` loop  
- Calculates sum of numbers from 1 to 10  
- Prints the result  

---

## 10. Combined Loop Practice
Write a program that:
- Uses `for` loop from 1 to 15  
- Skips multiples of 3 using `continue`  
- Stops loop if number becomes greater than 12 using `break`  
- Prints remaining numbers  

---
